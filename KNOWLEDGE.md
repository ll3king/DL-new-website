# 🏺 Dandy Lane Cafe - 项目核心知识库 (Digital Soul & Knowledge Base)

## 1. 架构愿景 (Architecture Vision)
- **核心理念**：低维护 (Low Maintenance)、极速加载 (Extreme Performance)、AI 优先 (AI First)。
- **技术栈**：
  - **静态前端**：使用 Nunjucks 模板生成的纯 HTML/CSS/JS。无 heavy 框架，确保最佳 SEO 和 AEO (AI 搜索引擎优化) 友好度。
  - **边缘计算 (Function-as-a-Service)**：Cloudflare Pages Functions。处理 AI 对话、预订逻辑、后台管理。
  - **数据库**：Google Sheets 作为主数据库。降低了非技术人员的管理门槛（Staff 只需要操作 Excel 表格即可管理订单）。

## 2. AI Concierge 指令集 (AI Logic & Persona)
AI Bot 的表现基于 `functions/api/chat.js` 中的 System Prompt。
- **身份认同**：定位为“Senior Staff”，禁止提及任何 AI 术语，旨在提供真人般的温暖感。
- **短期记忆**：通过前端 `chatHistory` 数组的往返传输实现上下文感知（Contextual Awareness）。
- **语言镜像 (Language Mirroring)**：识别并匹配用户的母语进行回复，提供尊贵的国际化服务体验。
- **静默熔断 (Silent Handoff)**：触发 `notify_management` 工具记录复杂问题。这是一种“掩护机制”，而不是报错。

## 3. 业务逻辑与安全规则 (Business Rules)
- **预订限额**：每小时最大 16 人。单次在线预订上限 6 人。
- **预订周期**：仅限“明天以后”的日期。今天及以前的诉求引导至 Walk-in（"Try our best to find seats"）。
- **资产管道**：`src/assets/media` 是唯一数据源。`npm run build` 会自动将其同步至 `public/assets`。
- **安全隔离**：所有密钥（API Keys, Google JSON）存放在 Cloudflare 环境变量中，绝不上传至代码库。

## 4. AEO (AI Engine Optimization)
- 项目包含了动态生成的 `JSON-LD` 结构化数据：
  - `Restaurant`：餐厅基本信息、地址、评分。
  - `FAQPage`：常见问题，提升 AI 回答命中率。
  - `Breadcrumb`：导航路径优化。
- 这些数据确保了 ChatGPT、Perplexity、Gemini 等 AI 在爬取网站时能精准捕捉 Dandy Lane 的特征。

## 5. 开发者维护备忘 (Maintenance Guide)
- **修改菜单/内容**：修改 `data/site.yaml` 或 `data/stories.yaml` -> `npm run build` -> `git push`。
- **修改 AI 语气**：编辑 `functions/api/chat.js` 中的 `systemPrompt` 字符串。
- **私钥注入补丁**：`functions/` 下的合约包含 `.replace(/\\n/g, '\n')` 补丁，确保从环境变量读取的 Google 私钥格式正确。

## 6. 未来展望 (Future Roadmap)
- **长期记忆**：引入 Cloudflare KV 存储，实现跨 Session 的个性化问候。
- **支付集成**：Stripe 端点预留。
- **全渠道集成**：将现有逻辑封装为 API，接入 WhatsApp 和微信商业版。

---
*Generated by Antigravity AI (Google DeepMind Special Agent) on 2026-02-27.*
