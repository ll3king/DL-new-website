# L3 - Assembly & Logic (SSG)
**Role:** The Engine.
**Allowed Imports:** `src/blocks` (L2), `data/` (L0).
**Responsibility:** Execute the build process. Read `src/pages/*.yaml`, fetch required Blocks, and render final HTML into `public/`.
