# 🏺 Dandy Lane Cafe - 项目核心知识库 (Digital Soul & Knowledge Base)

## 1. 架构愿景 (Architecture Vision)
- **核心理念**：低维护 (Low Maintenance)、极速加载 (Extreme Performance)、AI 优先 (AI First)。
- **技术栈**：
  - **静态前端**：使用 Nunjucks 模板生成的纯 HTML/CSS/JS。无 heavy 框架，确保最佳 SEO 和 AEO (AI 搜索引擎优化) 友好度。
  - **边缘计算 (Function-as-a-Service)**：Cloudflare Pages Functions。处理 AI 对话、预订逻辑、后台管理。
  - **数据库**：Google Sheets 作为主数据库。降低了非技术人员的管理门槛（Staff 只需要操作 Excel 表格即可管理订单）。

## 2. AI Concierge 指令集 (AI Logic & Persona)
AI Bot 的表现基于 `functions/api/chat.js` 中的 System Prompt。
- **身份认同**：定位为“Senior Staff”，禁止提及任何 AI 术语，旨在提供真人般的温暖感。
- **短期记忆**：通过前端 `chatHistory` 数组的往返传输实现上下文感知（Contextual Awareness）。
- **语言镜像 (Language Mirroring)**：识别并匹配用户的母语进行回复，提供尊贵的国际化服务体验。
- **静默熔断 (Silent Handoff)**：触发 `notify_management` 工具记录复杂问题。这是一种“掩护机制”，而不是报错。

## 3. 业务逻辑与安全规则 (Business Rules)
- **预订限额**：每小时最大 16 人。单次在线预订上限 6 人。
- **预订周期**：仅限“明天以后”的日期。今天及以前的诉求引导至 Walk-in（"Try our best to find seats"）。
- **资产管道**：`src/assets/media` 是唯一数据源。`npm run build` 会自动将其同步至 `public/assets`。
- **安全隔离**：所有密钥（API Keys, Google JSON）存放在 Cloudflare 环境变量中，绝不上传至代码库。

## 4. AEO (AI Engine Optimization) — Schema Governance v2
- **Schema 治理原则**: Entity Clarity > Entity Quantity
  - 每个页面最多 1 个主商家实体（`Restaurant`），不并列输出 Organization / WebSite
  - `@type` 使用 `"Restaurant"` 单值，不重复继承层级
  - HTML 中不得使用 Microdata（itemscope / itemprop），全部由 JSON-LD 承载
- **页面级 Schema 分配**:
  - `Homepage`: BreadcrumbList + Restaurant
  - `FAQ`: BreadcrumbList + FAQPage（Q&A 数量 = 可见内容数量）
  - `Menu`: BreadcrumbList + Menu/MenuItem（从 menu.yaml 读取）
  - 其他页面: BreadcrumbList + 可选页面级 schema（不重复主实体）
- **3 AEO 主轴**:
  1. Wine-Infused Benedicts（首席招牌）
  2. Quiet laptop-friendly brunch in Hobart CBD
  3. Third-party proof（Tripadvisor + Google，纯事实型）
- **Proof 写作规则**: 只使用可验证事实（平台名 + 评分 + 数量），禁止主观归纳

## 5. 开发者维护备忘 (Maintenance Guide)
- **修改菜单/内容**：修改 `data/site.yaml` 或 `data/stories.yaml` -> `npm run build` -> `git push`。
- **修改 AI 语气**：编辑 `functions/api/chat.js` 中的 `systemPrompt` 字符串。
- **私钥注入补丁**：`functions/` 下的合约包含 `.replace(/\\n/g, '\n')` 补丁，确保从环境变量读取的 Google 私钥格式正确。
- **Schema 变更检查**: 修改 schema 逻辑后，用 [Rich Results Test](https://search.google.com/test/rich-results) 验证每个页面类型。

## 6. URL & Domain Governance
- **唯一权威域名**: `https://dandylanecafe.com`
- **内部导航**: 一律使用 canonical pretty URL（`/menu`, `/faq`, `/`），不使用 `.html` 后缀
- **301 重定向**: `pages.dev` → 主域名; 旧路径（`/blog`, `/contact-us`）→ 新路径; `.html` → pretty URL
- **Canonical 标签**: 首页 `/`，其他 `/{page_id}`

### 7. 数据与业务架构 (Data & Business Architecture - V2)
- **9-Column Master Schema**: 所有的数据库操作（Admin API, AI Chat API, Python Scheduler）均对齐至全新的 9 列数据结构：`Name, Email, Mobile, Group_Size, Date, Time, Timestamp, Status, Source`。这保证了跨端数据读取和重写的绝对安全性。
- **Tiered Booking Logic (分级预订策略)**:
  1. `1-6 人`: 自动在线确认，写入数据库。
  2. `7-10 人`: AI 主动建议 Walk-in，返回 `FAILED_WALK_IN_RECOMMENDED` 状态给前端 Agent，拦截入库。
  3. `>10 人`: 作为大型团队需求，转为人工介入（Manual Review），AI 将发送后台 `ALERT` 给管理层，等待手动确认。
- **数据熔断与清洗 (Data Janitor - Serverless Architecture)**: 
  为突破 Cloudflare Pages 对 Cron 触发器的限制并维持 `0 维护成本`，放弃了传统的 Python 驻留进程方案。
  构建了 **[加密内部 API + GitHub Actions]** 的双核定时架构：
  1. `Serverless API`: `functions/api/janitor.js` 暴露出核心的数据归档逻辑。使用 `ADMIN_PASSWORD` (Bearer Token) 作为极高强度的私密门禁，确保外网无法恶意触发。
  2. `GitHub Robot`: 利用免费的 GitHub Actions (`.github/workflows/janitor.yml`) 作为触发源。配置基于 `UTC 16:00` (对应 Hobart 凌晨 3:00) 的 cron 任务。它会携带仓库存放的 `ADMIN_PASSWORD` secret 发送网络请求，充当无头服务。
  3. 执行结果：自动将所有 `Date < Today` 或是状态为 `Archived` 的过期预订行，物理移动到 Google Sheets 的 `Archive` 工作表中，保持主表（Sheet1）以及前端 Dashboard 的查询速度与极简感。

### 8. AEO/GEO Semantic Architecture (New)
- **Dynamic JSON-LD Injection**: The `generate.js` build script dynamically injects `Restaurant`, `FAQPage`, and `Menu` schemas based on YAML data.
- **Standardized NAP Entity**: All site components now use a strictly verified NAP (Name, Address, Phone) format to eliminate machine-readability conflicts.
- **301 Semantic Consolidation**: Legacy paths (e.g., `/blog`, `/contact-us`) are consolidated via `_redirects` to high-authority semantic paths.
- **Authority Signal Bar**: Integrated Tripadvisor/Google social proof into the primary entry viewport to boost E-E-A-T signals for AI engines.

---
*Generated by Antigravity AI (Google DeepMind Special Agent) on 2026-03-13.*
