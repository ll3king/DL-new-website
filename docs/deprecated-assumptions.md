# Deprecated Assumptions

Last updated: 2026-06-21

## Purpose

This file records old default assumptions that should no longer guide the Dandy Lane website project.

## Deprecated Defaults

### 1. SEO-first narrative

Deprecated because:

- the website is not governed as an SEO project
- search work now sits inside `AEO / GEO / AI Search Optimization`
- `SEO` is only infrastructure language

Replace with:

- two-flywheel governance
- `AEO / GEO / AI Search Optimization` as the strategic language

Deprecated-language rule:

- replace `SEO optimization` with `AEO / GEO / AI Search Optimization`
- `SEO` may only appear when describing technical infrastructure, not strategic direction

### 2. n8n default workflow

Deprecated because:

- it is not part of the current website build and design workflow
- it should not be introduced as a default solution path

Replace with:

- Stitch -> Codex via MCP -> repo implementation -> Taste / Impeccable visual QA -> performance QA

### 3. Google Sheets / Telegram automation as default website logic

Deprecated because:

- this is not the governing logic for the current website project

Replace with:

- direct repo-owned website governance and module boundaries

### 4. Gemini default workflow

Deprecated because:

- Gemini is not part of the current website workflow

Replace with:

- Stitch and Codex as the active design-and-implementation path

### 5. Figma default workflow

Deprecated because:

- Figma is not the default tool for this project phase

Replace with:

- Stitch as the primary design exploration canvas

### 6. Wedding / event / styling examples

Deprecated because:

- they pull the website toward the wrong visual and business cues
- they do not support appetite, brunch memory, or local discovery

Replace with:

- food-led, coffee-led, hidden-lane brunch direction

### 7. Release checks as the main project narrative

Deprecated because:

- release validation is important but not the main project driver

Replace with:

- Flywheel 2 as the current urgent priority

## Active Replacement Model

The current project should be understood through:

- Dandy Lane as a Hobart CBD hidden-lane breakfast-to-brunch cafe
- Flywheel 1: `AEO / GEO / AI Search Optimization`
- Flywheel 2: `Visual Taste / Frontend / Performance`

Current urgency belongs to Flywheel 2.
